---
title: "sqlstreams.consumer.cursor.backlog"
slug: errors/SS0083
kind: "metric"
consequence: "gauge of `{message}`, one series per consumer group"
---

Messages beyond the consumer group's committed frontier.

The `stream` and `group` attributes identify the series. The live snapshot
computes `head - committed`; growth means production is moving ahead faster
than the group resolves messages.
