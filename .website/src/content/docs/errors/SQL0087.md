---
title: "sqlstreams.consumer.exceptions.deferred"
slug: errors/SQL0087
kind: "metric"
consequence: "gauge of `{exception}`, one series per consumer group"
---

Delivery rows waiting for their message key's lease to become free.

The `stream` and `group` attributes identify the series. A row leaves deferred
after the key holder finishes and a later claim makes the row runnable.
