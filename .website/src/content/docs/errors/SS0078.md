---
title: "sqlstreams.alert.check.resolved_alerts"
slug: errors/SS0078
kind: "metric"
consequence: "gauge of `{alert}`, one series per alert check"
---

Alerts one run of the named check changed to resolved.

The `alert` attribute identifies the check. An already-resolved result does not
count as another change; each run replaces the retained value for that check.
