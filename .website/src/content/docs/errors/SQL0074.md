---
title: "sqlstreams.alert.state.resolved_alerts"
slug: errors/SQL0074
kind: "metric"
consequence: "gauge of `{alert}`, one series system-wide"
---

Retained alert heads whose latest status is resolved.

This counts resolved heads still retained on the alert stream. It is not a
lifetime total: a later active version for the same message key moves the head
back to the active gauge.
