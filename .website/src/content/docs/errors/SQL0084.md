---
title: "sqlstreams.consumer.cursor.inflight"
slug: errors/SQL0084
kind: "metric"
consequence: "gauge of `{message}`, one series per consumer group"
---

Claimed messages beyond the consumer group's committed frontier.

The `stream` and `group` attributes identify the series. The live snapshot
computes `claimed - committed`; backlog beyond this value has not yet crossed
the claimed frontier.
