---
title: "sqlstreams.consumer.abandoned_routines.total"
slug: errors/SQL0092
kind: "metric"
consequence: "gauge of `{routine}`, one series per consumer group"
---

Distinct abandoned consumerFunc routines retained on the metrics stream.

The `stream` and `group` attributes identify the series. A later cleared record
does not lower this total; retention can lower it after the abandoned event is
no longer present.
