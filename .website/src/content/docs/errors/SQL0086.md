---
title: "sqlstreams.consumer.exceptions.inflight"
slug: errors/SQL0086
kind: "metric"
consequence: "gauge of `{exception}`, one series per consumer group"
---

Delivery rows currently held by a retry attempt.

The `stream` and `group` attributes identify the series. These are exception
rows with an active lease, separate from the cursor range represented by
[`sqlstreams.consumer.cursor.inflight`](/errors/SQL0084).
