---
title: "sqlstreams.consumer.cursor.head"
slug: errors/SQL0080
kind: "metric"
consequence: "gauge of `{message}`, one series per consumer group"
---

The highest message id ever appended to the stream.

The `stream` and `group` attributes identify the series. Every group on one
stream sees the same head; it is repeated per group so it can be compared
directly with that group's claimed and committed frontiers.
