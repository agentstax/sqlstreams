---
title: "sqlstreams.alert.check.streams_evaluated"
slug: errors/SS0075
kind: "metric"
consequence: "gauge of `{stream}`, one series per alert check"
---

Streams one run of the named alert check evaluated.

The `alert` attribute separates partition count, compaction read cost, and
worker liveness. Each run replaces that check's retained value; this is not a
cumulative counter.
