---
title: "sqlstreams.schedule.state.suspended"
slug: errors/SQL0072
kind: "metric"
consequence: "gauge of `{found}`, one series system-wide"
---

Schedules currently excluded from production because they are suspended.

These rows are also excluded from the overdue count and oldest-due-age
calculation. The value is a current level, so resuming a schedule lowers it on
the next collection.
