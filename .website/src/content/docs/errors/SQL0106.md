---
title: "worker not found"
slug: errors/SQL0106
kind: "error"
recovery: "permanent"
consequence: "an unchanged retry cannot succeed; retry machinery stops immediately"
fix: "register the owning resource before operating its worker"
---

The stream exists but its maintenance worker has not been declared. Register
the stream through the current client, then retry the maintenance operation.
Registration preserves existing operational targets.
