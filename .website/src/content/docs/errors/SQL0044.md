---
title: "sqlstreams.consumer.session.superseded"
slug: errors/SQL0044
kind: "metric"
consequence: "counter of `{message}`, one series per consumer session"
---

Messages resolved without running because a newer version of their
compacted message key had already arrived.

A session flow: this instance's own running total, flushed to
`__system.metrics` each flush tick and printed as `superseded_count` on
the [consumer stopped](/errors/SQL0041) line. It never reconciles against
the fleet-wide `sqlstreams.consumer.*` gauges.
