---
title: "sqlstreams.consumer.exceptions.dead"
slug: errors/SQL0088
kind: "metric"
consequence: "gauge of `{exception}`, one series per consumer group"
---

Dead-lettered delivery rows retained for the consumer group.

The `stream` and `group` attributes identify the series. Dead rows no longer
block the committed frontier, but remain available for inspection and explicit
resolution.
