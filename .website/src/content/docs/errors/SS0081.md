---
title: "sqlstreams.consumer.cursor.claimed"
slug: errors/SS0081
kind: "metric"
consequence: "gauge of `{message}`, one series per consumer group"
---

The highest message id the consumer group has claimed through its cursor.

The `stream` and `group` attributes identify the series. Messages between
committed and claimed may still be running or waiting on individual exception
rows.
