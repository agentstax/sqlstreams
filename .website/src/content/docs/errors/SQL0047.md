---
title: "sqlstreams.consumer.session.dead"
slug: errors/SQL0047
kind: "metric"
consequence: "counter of `{message}`, one series per consumer session"
---

Delivery rows this instance wrote `'dead'`: exhausted retries,
unrecoverable payloads, and the crash-loop kill backstop. These sit
dead-lettered until resolved.

A session flow: this instance's own running total, flushed to
`__system.metrics` each flush tick and printed as `dead_count` on the
[consumer stopped](/errors/SQL0041) line. It never reconciles against the
fleet-wide `sqlstreams.consumer.*` gauges.
