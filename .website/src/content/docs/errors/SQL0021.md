---
title: "could not finish the stream declaration"
slug: errors/SQL0021
kind: "error"
recovery: "transient"
consequence: "an unchanged retry can succeed; retry machinery retries on its backoff schedule"
fix: "run Client.Stream(name).Register again if the stream should still exist"
---
