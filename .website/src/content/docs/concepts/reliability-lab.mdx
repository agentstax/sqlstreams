---
title: How SQLStreams tests reliability
description: An hour of real producers and consumers against a real Postgres, ending in one verdict. A SQL join proves no committed message went missing.
---

import ThreadAside from '../../../components/thread-aside/thread-aside.svelte';

SQLStreams promises at-least-once delivery with no loss. The reliability lab
is how that promise is checked: an hour of real producers and consumers
against a real Postgres, and one verdict at the end. Pass means every
committed message was delivered or dead-lettered and nothing else
happened. This page is the lab as it ships, written so you can judge what
"pass" actually proves, and then the chaos run it grows into.

<ThreadAside label="NOTE" title="What ships today">
`quiet` runs an hour at 200 messages a second with three consumer instances
and no chaos; `dev` runs one minute. The `multistream-1`, `-4`, and `-16`
scenarios step the offered rate across different stream counts. `throughput`
is a 30-second probe with 1 KB messages and automatic batching. `just reliability-lab dev` builds the stack, runs it, and
exits with the verdict. Sections headed "Proposed" describe work not yet built. The [demo page](/demo/) is the same idea
packaged for you to run in five minutes.

</ThreadAside>

## The tests around it

SQLStreams is tested at unit scale and at mechanism scale. `go test -race`
covers the packages. About twenty failure-injection e2e tests each run one
mechanism for a few minutes under a real Postgres: reclaim of an expired
lease, dead-lettering after the retry budget, shutdown that truncates a
batch mid-commit, exclusive keys, partition drops. A compat lab runs the
previous release against the current schema at every release checkpoint.

None of them run long, run everything at once, and account for every
single message. That is the reliability lab's job. A reclaim e2e test proves a reclaim
works; the lab proves that after an hour the total still adds up.

## A ledger and a checker

The lab is mostly bookkeeping. Postgres is the thing under test, so the
lab keeps its own books in a second schema on the same instance and the
final report is a SQL join between the two.

```
   producers                                              consumer instances
   ┌──────────┐   produce                       claim      ┌──────────┐
   │ p-1  p-2 │ ─────────────▶  sqlstreams.*  ◀───────────── │ c-1  c-2 │
   └──────────┘                 message_log                │ c-3      │
        │                       delivery_log               └──────────┘
        │ every attempt:        exception_queue                  │
        │ key, outcome                                           │ every handler call:
        ▼                                                        ▼ message id, outcome
   ┌─────────────────────────────────────────────────────────────────────┐
   │ lab.*   produce_record        handler_record        run_phase       │
   └─────────────────────────────────────────────────────────────────────┘
                                      │
                                      ▼  after producers stop and consumers drain
                                  the checker: one SQL join, one verdict
```

Every produce writes two facts to the ledger: that it was attempted, and
what came back. What comes back is one of three things. Committed, with
the idempotency key. Rejected, with the SS code. Or unknown, because the
connection died after the commit was sent and before the reply arrived.
Most test suites never model that third outcome; Kafka's own verifier has
no bucket for it. The lab resolves it after the run by looking the key up
in the message log: if the row is there the produce is counted as
recovered, if not it is nothing. Only a committed produce can be lost.

Every handler invocation writes one row too: message id, consumer group,
attempt, and whether the handler succeeded, failed, or the process died
before it could say.

After the producers stop, the consumers drain until the group's cursor
has passed the last message the stream holds, and then the checker runs. It sorts every produced message into a named bucket, and the
buckets must add up:

```
   produced  =  success  +  dead  +  compacted away  +  other schema version

   committed keys  ==  message_log rows           (nothing lost, nothing invented)
   every message   -> >= 1 delivery ending success or dead
   anything outside a named bucket                 -> FAIL
```

There is no acceptable "dropped" bucket. Every expected skip has a name,
and a message that lands in none of them fails the run.

Delivery has two witnesses. The handler records say the handler ran. The
committed cursor and exception rows say whether the library completed the
message or dead-lettered it. Both are needed: progress can pass a message
whose handler never ran, which fails `undelivered` even if `unbucketed`
passes. This also lets the checker run with failure-only delivery logging.

Duplicates are counted and reported, never failed. A consumer instance
killed between the handler returning and the commit is redelivered after
the lease expires, so a second success row for the same message is the
contract working. The [message lifecycle](/concepts/lifecycle/) page has
the redelivery window; the lab shows the count and where in the run each
one happened.

## A run reads like one file

Each run is described by a scenario file with three sections: what is
being tested, what happens over time, and what must be true at the end.
It is meant to be read cold in under a minute. The file is a description,
not a program. Each scenario is plain Go in the lab, written by hand, and
the report prints it back from that Go in this same format; a test diffs
the two, so a stale file fails the build. This is `quiet`:

```
# the hour-long quiet run: constant load, fixed consumers, no chaos, nothing may move

[input]
stream        orders           DeliveryLogMode all
consumers    fraud-scoring    handler fail rate 0, 3 retries then dead
duration     60m

[shape]
hold:    steady 200/s 60m

at 0m    consumers 3

[expect]
lost           0
unexpected     0
recovered      report
undelivered    0
duplicates     report
unbucketed     0
reclaims       0
dead           0
```

The six lines above `reclaims` are the safety checks. Every scenario must
declare them with these values, and the lab refuses one that drops a line.
`reclaims` and `dead` are this scenario's own: with no chaos and a fail
rate of zero, a reclaim or a dead-lettered message is a finding on its own.

## What pass means

The report is the scenario file printed back with the actual value beside
every expectation. This is the `dev` run from its record:

```
[expect]
lost           0         actual 0    PASS
unexpected     0         actual 0    PASS
recovered      report    actual 0
undelivered    0         actual 0    PASS
duplicates     report    actual 0
unbucketed     0         actual 0    PASS
reclaims       0         actual 0    PASS
dead           0         actual 0    PASS

records     24000 produce, 12000 handler, 3 phase rows
produced    attempted 12000, committed 12000, rejected 0, unknown 0
handled     success 12000, error 0
verdict     pass
```

A verdict has three values, not two. Pass, fail, and unknown. Unknown is
what a run reports when the checks could not be run to completion:
nothing was produced, the drain budget was spent before the group's
cursor caught up, a checker query errored. Later, it is also what a run
reports when the fault it was meant to exercise never happened. This is
the rule the lab borrows most deliberately from the suites that have
lasted: a test that passes while checking nothing is worse than one that
fails.

A green run is trusted only after sabotage. Deleting one `message_log` row
after the run turns `lost` into `actual 1  FAIL  key producer-1`. Deleting
one handler record line turns `undelivered` into
`actual 1  FAIL  message_id 2`. A run with no records reads
`unknown  nothing was produced`.

## Proposed: the chaos run

The next stage is the same hour with load changes and faults. Nothing in
this section is built; the scenario file is its spec.

```
# steady load, a pause, a saturating spike, slow ramp back, one kill

[input]
stream       orders           DeliveryLogMode all
consumers   fraud-scoring    handler fail rate 0.02, 3 retries then dead
duration    60m

[shape]
warm:       steady 200/s 10m
quiet:      pause 1m
flood:      saturate 64 in flight 5m
cooldown:   ramp 2000/s -> 200/s 10m
hold:       steady 200/s 34m

at 0m       consumers 3
at 20m      consumers 0
at 22m      consumers 8
at 45m      kill consumer 2

[expect]
lost          0
unexpected    0
recovered     report
undelivered   0
unbucketed    0
duplicates    report
reclaims      0 outside chaos windows
recovery      <= 2m after quiet, flood, and the kill
```

The producer phases run back to back. Consumer counts and chaos actions
land at times measured from the start. On a timeline the same run looks
like:

```
  0m            10m 11m    16m         26m                               60m
  ├─── warm ────┼─q─┼ flood ┼─ cooldown ─┼───────────── hold ──────────────┤
     200/s        0   64 in   2000 -> 200/s            200/s
                      flight

  consumers  3 ──────────────────────┐   ┌── 8 ──────────────x──────────────
                                  20m└─0─┘22m               45m  kill c-2
                                                                 a reclaim is
                                                                 expected here
                                                                 and nowhere else
```

Two phases are there on purpose. Consumers at zero for two minutes means
the backlog must grow and then drain with nothing lost. Eight consumers
on a stream that needs three means oversubscription, and the checker still
expects zero reclaims outside the kill.

The report would read:

```
[expect]
lost          0                   actual 0          PASS
unexpected    0                   actual 0          PASS
recovered     report              actual 0
undelivered   0                   actual 0          PASS
unbucketed    0                   actual 0          PASS
duplicates    report              actual 3          message_id 118402, 118403, 391115
reclaims      0 outside windows   actual 1 inside   PASS   (45m02s, c-2 range 118400..118463)
recovery      <= 2m               actual 41s, 1m12s, 58s    PASS

[faults]
consumers 0 at 20m    ran
kill consumer 2 at 45m    ran

verdict: PASS
```

The three duplicates are the messages consumer 2 had finished but not
committed when it was killed, redelivered after its lease expired. They
are listed, not failed. The one reclaim sits inside the kill window, so
it passes. The same reclaim at 30m would fail the run: with no chaos, a
reclaim means a consumer instance lost a lease it should have held.

A kill that lands on an idle instance, or a dev-length run whose lease
never expires, reads unknown: the fault the run was meant to exercise
never happened.

## Where it goes from here

The shipped version is deliberately small: one plain stream, one consumer
group, one load level, and the scenario file. Each stage adds one axis and
one named bucket:

1. Kills of consumers and producers mid-work, and pausing Postgres past
   the lease so every instance loses its lease at once.
2. A compacted stream with message keys, where "compacted away" becomes a
   real bucket and per-key order is checked.
3. Two schema versions live at once, so "other schema version" fills.
4. Routing bindings and fan-out, then schedules.
5. Metrics and alert assertions read from `__system` at the end.

The checker never changes shape through those stages. Each one adds a
bucket to the sum and a fault the run must prove it reached.

## Proposed: sustainable throughput

The throughput run measures how fast messages can be produced and consumed
at the same time without either queue growing. The first workload uses
1 KB messages, one consumer group, a handler with no external calls,
automatic produce batching, and failure-only delivery logging.

For `orders`, producing 10,000 messages a second requires its group to
finish 10,000 distinct messages a second. Adding `analytics` as a second
group requires another 10,000 deliveries a second. Report the produce
rate and each group's completion rate separately.

Caller-supplied idempotency keys use individual transactions. They are a
separate workload; changing the batch configuration does not tune that
path. The automatically batched workload still uses the library's durable
produce path.

Short probes find useful settings. A sustainable result needs three
15-minute measured windows after warmup, no growing producer or consumer
queue, and 99% of messages handled within one second of their scheduled
send time. Checkpoints, vacuum, and retention cleanup must occur during
validation. Draining after production stops only proves eventual completion.

On the development Mac, all benchmark storage is limited to 20 GiB with
40 GiB left free on the host. Database files, WAL, records, checker imports,
and temporary files count. Retention is chosen from measured growth before
long runs. Durable commits, fsync, full-page writes, and autovacuum stay on.

Scenario-file stream declarations also accept `PartitionSize`, using the
library's existing stream setting. Zero keeps the default; an explicit value
appears in the run's declaration. Larger partitions reduce how often new
partitions are created. A short run that never crosses a partition boundary
cannot establish throughput during rollover.
