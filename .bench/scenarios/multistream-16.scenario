# the saturation ladder over 16 streams: the same total rate stepped up per rung until a guard gives, two groups on every stream

[input]
stream       orders-1         DeliveryLogMode all
consumers    fraud-scoring    handler fail rate 0, 3 retries then dead, batch 100
consumers    analytics        handler fail rate 0, 3 retries then dead, batch 100
stream       orders-2         DeliveryLogMode all
consumers    fraud-scoring    handler fail rate 0, 3 retries then dead, batch 100
consumers    analytics        handler fail rate 0, 3 retries then dead, batch 100
stream       orders-3         DeliveryLogMode all
consumers    fraud-scoring    handler fail rate 0, 3 retries then dead, batch 100
consumers    analytics        handler fail rate 0, 3 retries then dead, batch 100
stream       orders-4         DeliveryLogMode all
consumers    fraud-scoring    handler fail rate 0, 3 retries then dead, batch 100
consumers    analytics        handler fail rate 0, 3 retries then dead, batch 100
stream       orders-5         DeliveryLogMode all
consumers    fraud-scoring    handler fail rate 0, 3 retries then dead, batch 100
consumers    analytics        handler fail rate 0, 3 retries then dead, batch 100
stream       orders-6         DeliveryLogMode all
consumers    fraud-scoring    handler fail rate 0, 3 retries then dead, batch 100
consumers    analytics        handler fail rate 0, 3 retries then dead, batch 100
stream       orders-7         DeliveryLogMode all
consumers    fraud-scoring    handler fail rate 0, 3 retries then dead, batch 100
consumers    analytics        handler fail rate 0, 3 retries then dead, batch 100
stream       orders-8         DeliveryLogMode all
consumers    fraud-scoring    handler fail rate 0, 3 retries then dead, batch 100
consumers    analytics        handler fail rate 0, 3 retries then dead, batch 100
stream       orders-9         DeliveryLogMode all
consumers    fraud-scoring    handler fail rate 0, 3 retries then dead, batch 100
consumers    analytics        handler fail rate 0, 3 retries then dead, batch 100
stream       orders-10        DeliveryLogMode all
consumers    fraud-scoring    handler fail rate 0, 3 retries then dead, batch 100
consumers    analytics        handler fail rate 0, 3 retries then dead, batch 100
stream       orders-11        DeliveryLogMode all
consumers    fraud-scoring    handler fail rate 0, 3 retries then dead, batch 100
consumers    analytics        handler fail rate 0, 3 retries then dead, batch 100
stream       orders-12        DeliveryLogMode all
consumers    fraud-scoring    handler fail rate 0, 3 retries then dead, batch 100
consumers    analytics        handler fail rate 0, 3 retries then dead, batch 100
stream       orders-13        DeliveryLogMode all
consumers    fraud-scoring    handler fail rate 0, 3 retries then dead, batch 100
consumers    analytics        handler fail rate 0, 3 retries then dead, batch 100
stream       orders-14        DeliveryLogMode all
consumers    fraud-scoring    handler fail rate 0, 3 retries then dead, batch 100
consumers    analytics        handler fail rate 0, 3 retries then dead, batch 100
stream       orders-15        DeliveryLogMode all
consumers    fraud-scoring    handler fail rate 0, 3 retries then dead, batch 100
consumers    analytics        handler fail rate 0, 3 retries then dead, batch 100
stream       orders-16        DeliveryLogMode all
consumers    fraud-scoring    handler fail rate 0, 3 retries then dead, batch 100
consumers    analytics        handler fail rate 0, 3 retries then dead, batch 100
producer     batch concurrency 16 per stream
duration     8m

[shape]
rung-2000:     steady 125/s per stream 2m
rung-4000:     steady 250/s per stream 2m
rung-8000:     steady 500/s per stream 2m
rung-16000:    steady 1000/s per stream 2m

at 0m    consumers 2

[expect]
lost                  0
unexpected            0
recovered             report
undelivered           0
duplicates            report
unbucketed            0
reclaims              report
dead                  0
schedule_kept         report
backlog_bounded       report
generator_headroom    report
